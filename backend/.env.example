# Backend Configuration
# Most settings have sensible defaults in config.py

# Database (default: SQLite)
DATABASE_URL=sqlite:///./video_generator.db

# Redis (required for job queue)
REDIS_URL=redis://localhost:6379/0

# CORS origins (add your frontend URL)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Application
DEBUG=true
API_HOST=0.0.0.0
API_PORT=8000

# API Keys (add your keys here for full functionality)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
REPLICATE_API_KEY=
ELEVENLABS_API_KEY=
GEMINI_API_KEY=

# Music Video Module
MV_DEBUG_MODE=false

# Mock Video Generation Mode
# When enabled, video generation endpoints return pre-staged mock videos
# instead of calling external APIs. Useful for frontend development and testing.
MOCK_VID_GENS=false

# Mock Video Generation Delay (in seconds)
# Control the simulated processing delay when MOCK_VID_GENS is enabled
# The actual delay will be a random value between MIN and MAX
MOCK_VIDEO_DELAY_MIN=5.0
MOCK_VIDEO_DELAY_MAX=10.0